<!DOCTYPE html>
<html>
  <head>
    <title>Aula</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
  <% if utilizador_signed_in?%>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
      <a class="navbar-brand" href="<%= root_url%>">PontPoint</a>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
          <li class="nav-item active">
            <a class="nav-link" href="<%= turmas_path %>">Turmas <span class="sr-only">(current)</span></a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="#">Marcações</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="#">Utilizadores</a>
          </li>
          <li class="nav-item">
          <%= link_to "Log out", destroy_utilizador_session_path, :method => :delete, class: 'nav-link' %>
          </li>
          <li class="nav-item">
          <%= current_utilizador.email %>
          </li>
        </ul>
      </div>
    </nav>
    <div class="container">
      <%= notification %>
      <%= yield %>
    </div>
  <% else %>
    <div class="container">
      <%= yield %>
    </div>
  <% end %>

    
  </body>
</html>
